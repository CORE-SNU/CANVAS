[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "canvas"
version = "0.0.0"
description = "Local editable install for CANVAS"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
include = ["canvas*"]